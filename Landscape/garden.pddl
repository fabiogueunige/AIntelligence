;Header and description

(define (domain garden)

;remove requirements that are not needed
    (:requirements :strips)


    ; un-comment following line if constants are needed
    ;(:constants )

    (:predicates 
        (VASE ?v)(FLOWER ?f)(SPOT ?loc)
        
    )
    (:action full_vase
        :parameters (?loc ?f ?v)
        :precondition (and ())
        :effect (and )
    )



)